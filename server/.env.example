# 考研备考台推送后端环境变量示例
# 复制为 .env 后填入真实值（运行 node gen-vapid.js 自动生成 VAPID 密钥）

PORT=8080

# VAPID 密钥（必填，运行 node gen-vapid.js 生成）
VAPID_PUBLIC_KEY=__FILL_ME__
VAPID_PRIVATE_KEY=__FILL_ME__
VAPID_SUBJECT=mailto:admin@example.com

# 状态持久化目录（默认 ./data）
STATE_DIR=./data
