# 考研备考台 · 跨设备真后台推送 · 后续操作步骤指引

> 适用版本：v6.3（已内置自托管 Node 后端模块 + PWA 套件）。
> 本指引目标：把"已写好的代码"变成"真正可用的跨设备后台推送"。

---

## 一、背景状态（当前已完成 / 未完成）

**已完成（代码层面，v6.3）**
- 主程序 `考研备考台.html`：任务调度（顺延+合并+每日硬封顶 5 项）、结构化计划、提醒中心、知识点卡片（艾宾浩斯 9 档）、错题本、模考趋势、目标完成度、PWA+SW 应用内兜底提醒。
- 后端 `server/`：`express + web-push + VAPID`，含 `/api/vapid`、`/api/sync`（共享状态按 ws 隔离 LWW）、`/api/subscribe`，以及 **60 秒调度器**（到点读共享数据推送到期卡片）。
- 自托管 PWA 套件 `考研备考台-pwa/`：`index.html`、`sw.js`（含 push 事件）、`manifest.webmanifest`、`icon.svg`、`README.md`。
- 本地冒烟测试通过：`PORT=8099` 下所有端点返回 200。

**未完成（需你执行部署/接线，因为涉及"自有 HTTPS 域名 + 自定义 sw.js + 后端"——平台托管链接 `workbuddy.link` 三者皆不满足，浏览器标准限制，非 bug）**
- VAPID 密钥：后端首次启动**自动生成**（无需手动），也可自带 `server/.env` 覆盖。
- 后端未部署到公网 HTTPS 服务。
- 自托管 PWA 未托管到与后端同源的 HTTPS 站点。
- 前端 `_BE_URL` 仍指向占位，未指向你的后端。
- 未在任何设备安装 PWA 并授权推送订阅。

---

## 二、最终目标

让「考研备考台」实现**真正的跨设备后台推送**：
- 在任意设备（关闭网页/App 后）到点自动弹出**含答案**的复习提醒；
- 多设备通过后端共享状态保持数据同步；
- 真后台仅在「自托管 PWA + 后端」生效；平台链接版保留应用内 20s 轮询兜底。

---

## 三、按顺序执行的具体动作

| 步骤 | 动作（直接执行） | 预期产出 | 约束 / 注意 |
|---|---|---|---|
| **S1 选平台** | 注册支持**常驻 Node 进程 + 自动 HTTPS** 的免费平台：推荐 **Render.com** 或 **Railway.app**（均自带 `*.onrender.com` / `*.up.railway.app` HTTPS）。仓库已内置 `render.yaml` / `railway.json` / `railway.toml` / 根 `package.json`，连仓库即按配置部署。避免 Vercel（serverless 无常驻进程）。 | 获得 HTTPS 基址，如 `https://kaoyan.onrender.com`。 | 必须自有/平台子域 HTTPS；平台托管链接不可用。 |
| **S2 生成密钥（可选）** | 默认**不需手动**：后端首次启动自动生成并保存 VAPID 密钥到 `STATE_DIR/vapid.json`。如需自带密钥，进 `server/` 执行 `node gen-vapid.js` 写入 `server/.env` 覆盖。 | 自动：日志「🔑 已自动生成并保存 VAPID 密钥」；自带：`server/.env` 含两密钥。 | 私钥勿提交仓库（`.gitignore` 已含）；想要密钥稳定可在平台挂持久磁盘到 `STATE_DIR`。 |
| **S3 一键部署（后端 + 同源 PWA）** | 把【整个仓库】推到 Git → Render 建 **Blueprint** 连仓库 / Railway 连仓库 → 平台按配置文件自动 `build`(`cd server && npm install`) 与 `start`(`cd server && node index.js`)。后端会**同源静态托管 PWA**，无需单独部署前端。 | `https://<你的域名>/` 直接打开应用并可安装；`/api/vapid` 返回公钥；`/api/sync`、`/api/subscribe` 均 200。 | 服务需**常驻**（非 serverless）跑 60s 调度器；无需手动填任何环境变量（VAPID 零配置）。 |
| **S4 （可选）独立托管 PWA** | 若想用独立 CDN（Netlify/Cloudflare Pages）托管 PWA，须与后端**严格同源**（同域名），否则 `sw.js` 注册失败。多数情况跳过——S3 已同源托管。 | 同源站点可打开并安装 PWA。 | `sw.js` 必须与页面同源；不同源则后台失效。 |
| **S5 接线前端（通常免改）** | `_BE_URL` 默认 = 页面同源（`location.origin`），S3 同源场景下**无需修改**。仅当 S4 独立托管时才把 `_BE_URL` 改为后端地址；`PUSH_VAPID_PUBLIC_KEY` 始终留空（自动从 `/api/vapid` 拉）。 | 自托管 HTTPS 下 `beEnabled()` 返回 `true`；平台链接版仍 `false`（正常）。 | 改完需重新部署 PWA。 |
| **S6 安装并授权** | 浏览器打开 PWA → **安装到主屏/桌面** → 设置页开「浏览器通知」→ 允许推送授权（自动 `beSubscribe` 上报订阅）。 | 后端 `/api/subscribe` 收到订阅；**关闭网页后到点仍弹出含答案的复习提醒**。 | iOS Safari 需 **iOS 16.4+ 且已"添加到主屏幕"安装**；否则降级应用内轮询。 |
| **S7 多端同步验证** | 设备 A 打卡/加卡片 → 后端 `/api/sync` 收到上报；设备 B 打开 PWA → `bePull` 拉取共享状态。 | 两设备数据一致；各自按时收到推送。 | 默认 `ws` 隔离（同一 workspace 共享）；"扫码配对多端"需另行改造。 |

> 📦 **零配置已落地**：仓库根目录新增 `render.yaml`（Render 蓝图）、`railway.json` + `railway.toml`（Railway）、根 `package.json`（平台探测）。三者预置了构建/启动命令与可选环境变量占位；按 S3 把仓库连到平台即自动部署，**全程无需手动填写任何密钥**——VAPID 由后端首次启动自动生成并持久化到 `STATE_DIR`。

---

## 四、可选：本地快速验证（无需域名，仅测试）

1. 本机 `cd server && PORT=8099 node index.js`。
2. 用 `cloudflared tunnel --url http://localhost:8099`（或 ngrok）把本地暴露为公网 HTTPS 地址。
3. 手机访问该 HTTPS 地址，走 **S4–S6**（把 `_BE_URL` 指向隧道地址）。
4. 验证跨设备推送打通后即关闭隧道。

**约束**：隧道地址每次会变，仅适合联调，不适合长期正式使用。

---

## 五、关键约束汇总（务必遵守）

1. **浏览器标准铁律**：真后台 Web Push 必须同时满足 ① 自有 HTTPS 域名 ② 自定义 `sw.js` ③ 后端发推送；平台托管链接 `workbuddy.link` 三者皆无 → 该链接只能应用内轮询。
2. **VAPID 私钥保密**：泄露等于任何人可冒充你发推送；`.env` 已 gitignore。
3. **后端需常驻进程**：Render/Railway/Fly 等；Vercel serverless 需改写为 Cron 定时调用推送函数（工作量大）。
4. **iOS 限制**：仅安装版（添加到主屏幕）iOS 16.4+ 支持；否则降级。
5. **同源约束**：`sw.js` 与页面同源，否则注册失败、后台失效。
6. **数据隔离**：`ws`（workspace）决定谁共享数据；默认同一 ws 共享，跨用户需换 ws。

---

## 六、我可继续代劳的部分（按需告知）

- ✅ 已生成 **Render / Railway 一键部署配置**（`render.yaml` / `railway.json` / `railway.toml`），零配置部署，无需手动填环境变量。
- 若你坚持 Vercel，改写为 **Vercel Cron + Serverless Function** 推送方案。
- 实现 **"扫码配对多端"**（把 `ws` 改为扫码传递，而非默认共享）。
- 把 `考研备考台.html` 主程序与 PWA 套件做一次最终同步发布。
