# 考研备考台 · 缺口补全方案

> 配套文件：`考研备考台.html`（工作台本体）、`考研备考-周计划总览.md`（逐周计划）。
> 本文说明**当前已实现 / 部分实现 / 缺失**的模块，并明确本次已补全模块与待补全模块的**实现目标与范围**。

## 一、现状评估（模块清单）

| 模块 | 状态 | 说明 |
|------|------|------|
| ① 每日任务清单 | ✅ 已实现 | 按「阶段 + 星期模板」自动生成当天四科任务，数据结构含代码/费曼标注 |
| ② 可视化图表 | ✅ 已实现 | 总完成率圆环 + 四科进度条 + 四科阶段甘特图（标「今天」线） |
| ③ 每日打卡 | ✅ 已实现 | 点选即勾，本地存储，连续打卡天数，逾期标红不消失 |
| ④ 笔记与心情 | ✅ 已实现 | 按日期存学习笔记 + 5 档心情 emoji，自动保存 |
| ⑤ 周报 / 月报 / 全程 | ✅ 已实现 | 自动汇总完成率、各科完成度、有笔记天数、心情分布、本周动态调参结论 |
| ⑥ 动态调参 | 🟡 部分实现 | 按上周完成率 ±10% 调系数（0.7–1.3），仅影响时长与单科加练；**调节结论原先仅在报告里一行字，不可见、不可信** |
| ⑦ 背景信息 | 🟡 本次补全 | 原为「在读」，已改为**已毕业（在职）**；缺口文案/证书时间线重适配 |
| ⑧ 缺口分析 + 证书 | 🟡 本次补全 | 6 条缺口卡 + 8 项证书，文案已改为工作党可执行节奏 |
| ⑨ 多端同步 | 🟡 部分实现 | 导出/导入 JSON 兜底；**非实时**，在线链接两端独立 |
| ⑩ 周计划总览 | 🆕 本次新增 | 按周展示全程重点与阶段推进方向（基础→进阶），当前周高亮 |
| ⑪ 目标分数分解 | ❌ 缺失 | 四科目标分（如 408≥70）未设定，无「进度 vs 目标」对比 |
| ⑫ 提醒 / 推送 | ❌ 缺失 | 仅外部过期自动化，App 内无提醒；且自动化仍指向旧考试日 |
| ⑬ 离线安装 PWA | ❌ 缺失 | 页面无外部依赖可离线，但无 manifest/service worker，加到主屏无图标 |

---

## 二、本次已补全的模块（目标与范围）

### 模块 ⑩ 周计划总览（新增）
- **实现目标**：让用户在备考全程随时看到「每周重点 + 阶段推进方向」，建立从**基础巩固 → 强化提升 → 冲刺应用**的整体认知，避免「只盯今天、看不清全局」。
- **范围**：
  - 新增第 6 个底部 Tab「🗓️ 周计划」；
  - 按三阶段分组展示 **W0 ~ W71** 每周的日期范围、各科重点主题（与「今日任务」**同源生成**，保证一致）、动态系数；
  - 当前周高亮；提供「展开全部 / 仅看当前阶段」切换；
  - 每周主题由 `weekFocusPreview()` 聚合该周 7 天任务去重得到，零额外维护。
- **边界**：展示层只读，不修改计划；真正改任务量仍由动态调参在生成时完成。

### 模块 ⑦⑧ 毕业（在职）背景适配
- **实现目标**：把「26 届在读」改为「已毕业 · 在职」，使缺口分析与证书时间线符合**已工作、无法重修刷分、可走社会考生报四级**的真实约束。
- **范围**：
  - 默认背景 `grade` 改为「已毕业（26届 · 2026年6月毕业 · 在职）」；
  - 6 条缺口卡重写：②挂科明确「已毕业不可重修，用工作项目/开源/竞赛弥补」；③英语明确「社会考生报 CET-4」；⑥科研改为「工作即项目经历」；
  - 8 项证书建议时间从「大三上/大三下」改为「今年12月/明年6月/年内/明年省赛」等工作党节奏。
- **边界**：仅文案与默认值；用户仍可在「缺口 → 编辑」手动改背景。

### 模块 ⑥ 动态调参可视化增强
- **实现目标**：让「自动调节」被看见、被信任，用户能理解为什么某周任务变多/变少。
- **范围**：在周计划模块每週卡片直接展示**系数 + 调节结论**（≥85% 加量 10% / <60% 保底 10% / 中间维持）；未来周显示「系数沿用上周，本周生成时按完成率自动调节」或「基准周」。
- **边界**：仍不改变核心算法（已验证：高完成率 → W1 系数 1.0→1.1 生效）；仅增加可读展示。

---

## 三、待补全模块（目标、范围与方案）

### 模块 ⑪ 目标分数分解
- **目标**：设定四科目标分（默认 408≥70、数一/英一/政过国家线或院校线），在可视化中对比「当前进度达成度 vs 目标」。
- **范围**：设置中增加 `targets{408,数1,英1,政}` 字段；可视化增加「目标达成进度条」；报告增加「距目标还差多少」。
- **方案**：纯前端，复用现有 `SUBJ` 与进度条组件，1–2 小时可落地。

### 模块 ⑨ 实时多端同步（升级路线）
- **目标**：手机 / 电脑 / 在线链接数据**秒级一致**，无需手动导出导入。
- **范围**：将 `checkins / notes / plan.weekly / certs` 从 `localStorage` 改为云端数据表读写；冲突按「日期 + 任务ID」**增量合并**（非整文件覆盖）。
- **方案**：待「资料库数据表 SDK」契约就绪后替换存储层；现有 JSON 导出/导入保留为离线兜底与迁移通道。**当前为已知最大缺口**。

### 模块 ⑫ 提醒 / 推送
- **目标**：每日推送当日任务 + 打卡提醒（App 内 + 可选外部），替代已过期自动化。
- **范围**：App 内「今日」页置顶当日倒计时与未完成项；外部提醒接自动化（更新至新考试日 2027-12-24）。
- **方案**：更新现有 `408每日提醒` 自动化至新考试日与四科内容；或待同步就绪后用云端定时任务。

### 模块 ⑬ 离线安装 PWA
- **目标**：加到主屏后有图标、无网络也能开，体验更接近原生 APP。
- **范围**：增加 `manifest.webmanifest` + 极小 `service-worker.js`（缓存单文件 HTML）。
- **方案**：页面已无外部依赖，天然可离线；加 manifest 即可。低优先级。

---

## 四、实现优先级

| 优先级 | 模块 | 原因 |
|--------|------|------|
| P0（已完成） | ⑩ 周计划总览、⑦⑧ 毕业适配、⑥ 调参可视化 | 你本次明确要求 + 修复时区 bug |
| P1 | ⑪ 目标分数分解 | 低成本高价值，让「过线」可量化 |
| P1 | ⑨ 实时同步 | 多端使用痛点，依赖 SDK 就绪 |
| P2 | ⑫ 提醒推送 | 更新过期自动化即可先顶上 |
| P2 | ⑬ PWA 离线安装 | 体验优化，锦上添花 |

> 当前版本（`kaoyan_desk_v3`）已包含 P0 全部内容，并通过逻辑校验（500 天 / 1218 任务零异常、动态调参生效、修复 GMT+8 时区导致的日期错位 bug）。
